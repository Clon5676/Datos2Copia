version: "3"
services:
  mysql:
    image: mysql:latest
    container_name: mysql-datosProyecto
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: socialDareDB
    ports:
      - "3306:3306"
